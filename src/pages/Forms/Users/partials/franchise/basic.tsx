import { TBlock } from "../../../../../utils/@types/components/Form"

type Props = {
  form: any
}

export const basicFranchise = ({ form }: Props): TBlock["groups"] => {
  const content: TBlock["groups"] = []

  return content
}
